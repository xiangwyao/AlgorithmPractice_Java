public class IsUnique {
    // Implement an algorithm to determine is a string has all unique characters. What if you cannot use additional
    // data structures?

    // We can form a hash table --> generate a hash code and try to insert into the hash table. If they are all unique,
    // there will be no conflicts.

    public static int generateHash(char c){
        int hashcode = 0;

        System.out.println();
        return hashcode;
    }

    public static void main(String[] args) {
        System.out.println("Hello World!");
    }

}
